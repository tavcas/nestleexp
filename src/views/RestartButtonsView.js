import './Game.css';

const RestartButtonsView = () => (
    <header className="game-header">
        <div className="game-title">Imagen original</div>
    </header>
);

export default RestartButtonsView;
